import AppLayout from '../components/common/AppLayout';
import CreatePost from '../components/posts/CreatePost';

const Root = () => {
  return (
    <AppLayout>
      <CreatePost />
    </AppLayout>
  );
};

export default Root;
